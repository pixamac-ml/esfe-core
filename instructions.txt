OBJECTIF

Améliorer le système de cours basé sur EC pour ajouter une structure pédagogique avec chapitres.

---

IMPORTANT

* ne pas casser EC existant
* améliorer ECContent
* ajouter une hiérarchie propre

---

PARTIE 1 — MODELS

Créer modèle :

```python
class ECChapter(models.Model):
    ec = models.ForeignKey("EC", on_delete=models.CASCADE, related_name="chapters")
    title = models.CharField(max_length=255)
    order = models.PositiveIntegerField(default=0)
```

---

Modifier ECContent :

```python
class ECContent(models.Model):
    chapter = models.ForeignKey("ECChapter", on_delete=models.CASCADE, related_name="contents")
    title = models.CharField(max_length=255)
    content_type = models.CharField(max_length=20, choices=[
        ("pdf", "PDF"),
        ("video", "Vidéo"),
        ("doc", "Word"),
        ("excel", "Excel"),
        ("text", "Texte"),
    ])
    file = models.FileField(upload_to="courses/", null=True, blank=True)
    video_url = models.URLField(null=True, blank=True)
    order = models.PositiveIntegerField(default=0)
```

---

PARTIE 2 — VUES

Créer :

* student_courses(request)
* ec_detail(request, ec_id)

---

Dans ec_detail :

* récupérer EC
* récupérer chapters triés
* récupérer contents triés

---

PARTIE 3 — TEMPLATE

Afficher :

* EC titre
* liste chapitres
* sous chaque chapitre → contenus

---

PARTIE 4 — AFFICHAGE CONTENU

Si :

* PDF → lien download
* vidéo → iframe ou lecteur
* texte → affichage direct

---

PARTIE 5 — DASHBOARD

Intégrer dans dashboard étudiant :

* liste EC
* clic → détail EC

---

OBJECTIF FINAL

Permettre :

* navigation par matière
* organisation par chapitre
* accès aux contenus pédagogiques
